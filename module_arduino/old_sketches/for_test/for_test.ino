const byte sizeArray = 8;
int array[sizeArray];

void setup() {
  for (int i = 0, p = 6; i < sizeArray; i++, p++) {
    array[i] = p;
  }
}

void loop() {
  // put your main code here, to run repeatedly:

}
